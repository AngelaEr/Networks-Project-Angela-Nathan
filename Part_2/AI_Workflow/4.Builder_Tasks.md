# Builder Tasks - משימות לביצוע
## Group_AN

**Status:** Completed
**Scope:** Code refinement in `Dashboard/Work_Space/ChatApp/` only

---

## Task 1: Add Code Comments (Hebrew)

### 1.1 Add header comment to server.py
**File:** `ChatApp/server/server.py`

Replace the existing docstring at top with:
```python
"""
שרת צ'אט - Chat Server
Group_AN - פרויקט גמר ברשתות תקשורת מחשבים

שרת TCP שמטפל בבקשות HTTP (קבצים סטטיים) וחיבורי WebSocket (צ'אט בזמן אמת)
משתמש בספריות מובנות בלבד: socket, threading, hashlib, base64, struct
"""
```

### 1.2 Add header to http_handler.py
**File:** `ChatApp/server/http_handler.py`

Replace existing docstring with:
```python
"""
מודול HTTP - HTTP Handler Module
Group_AN - פרויקט גמר ברשתות תקשורת מחשבים

מנתח בקשות HTTP ומגיש קבצים סטטיים (HTML, CSS, JS)
ללא frameworks - מימוש טהור ברמת socket
"""
```

### 1.3 Add header to websocket_handler.py
**File:** `ChatApp/server/websocket_handler.py`

Replace existing docstring with:
```python
"""
מודול WebSocket - WebSocket Handler Module
Group_AN - פרויקט גמר ברשתות תקשורת מחשבים

מימוש פרוטוקול WebSocket לפי RFC 6455
כולל: handshake, קידוד/פענוח frames, masking
"""
```

### 1.4 Add header to client_manager.py
**File:** `ChatApp/server/client_manager.py`

Replace existing docstring with:
```python
"""
מודול ניהול לקוחות - Client Manager Module
Group_AN - פרויקט גמר ברשתות תקשורת מחשבים

ניהול thread-safe של לקוחות מחוברים
כולל: הוספה, הסרה, ושידור הודעות לכולם
"""
```

---

## Task 2: Create docs/ README

### File: `ChatApp/docs/README.md`

Create with this content:
```markdown
# ChatApp - תיעוד
## Group_AN

אפליקציית צ'אט בזמן אמת מבוססת WebSocket

### מבנה הקבצים

```
ChatApp/
├── server/           # קוד השרת (Python)
├── client/           # קוד הלקוח (HTML/CSS/JS)
└── docs/             # תיעוד (אתה כאן)
```

### למידע נוסף
ראה `README.md` בתיקיית Work_Space
```

---

## Task 3: Verify Error Handling

Check that these error cases are handled (document findings):
- [x] Client disconnects unexpectedly → handled in `on_close()` (server.py:61-67)
- [x] Invalid WebSocket frame → handled in `decode_frame()` (websocket_handler.py:74-123)
- [x] File not found (404) → handled in `build_404()` (http_handler.py:139-142)
- [x] Directory traversal attack → blocked in `serve_static_file()` (http_handler.py:111-119)

**Note:** All verified and working correctly.

---

## Checklist

| Task | Status |
|------|--------|
| Add Hebrew header to server.py | [x] |
| Add Hebrew header to http_handler.py | [x] |
| Add Hebrew header to websocket_handler.py | [x] |
| Add Hebrew header to client_manager.py | [x] |
| Create ChatApp/docs/README.md | [x] |
| Verify error handling | [x] |

---

## Files to Modify

```
ChatApp/
├── server/
│   ├── server.py              ← Update header
│   ├── http_handler.py        ← Update header
│   ├── websocket_handler.py   ← Update header
│   └── client_manager.py      ← Update header
│
└── docs/
    └── README.md              ← Create new
```
